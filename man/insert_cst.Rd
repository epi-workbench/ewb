% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insert_cst.R
\name{insert_cst}
\alias{insert_cst}
\title{Launch the Insert-CST Gadget (RStudio Addin)}
\usage{
insert_cst()
}
\value{
Invisibly returns the selected template's function name (character).
}
\description{
Launches a small UI to select from available Code Submission Test
(CST) insertion templates (e.g., \code{insert_cst_blanks()}) and inserts the chosen
template at the current cursor location in the active RStudio document.
}
\details{
\itemize{
\item Designed as a thin wrapper around existing insertion helpers.
\item New templates can be registered by adding an entry to \code{cst_templates()} (see below).
\item Requires the RStudio IDE.
}
}
\section{Extending}{

Add a new template by defining a function that performs the insertion
(e.g., \code{insert_cst_mytemplate()}), then register it in \code{cst_templates()}.
}

\examples{
\dontrun{
  insert_cst()  # Launch the gadget
}

}
